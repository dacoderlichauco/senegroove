export type Gem = {
  x: number;
  y: number;
  size: number;
  speed: number;
  time: number;
};

export type Score = {
  hits: number;
  misses: number;
  earlyHits: number;
};

export type GemData = {
  TIME: string;
  LABEL: string;
};